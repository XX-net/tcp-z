If a external language file exists, TCP-Z will use it first. 
Otherwise, try to use internal resources.

The language file must save as unicode encoding.

The language ID can be found in language window.